services:
  traefik:
    build:
      context: ./traefik
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "${TRAEFIK_HTTP_PORT:-80}:80"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - edge

  web:
    build:
      context: ./web
    networks:
      - edge
    labels:
      - traefik.enable=true
      - traefik.http.routers.web.rule=PathPrefix(`/`) && !PathPrefix(`/api`)
      - traefik.http.routers.web.entrypoints=web
      - traefik.http.services.web.loadbalancer.server.port=80
      - traefik.http.routers.web.priority=1

  order-processor:
    build:
      context: ./order-processor
    environment:
      # Event Hub consumer (LISTEN)
      - EVENTHUB_CONNECTION_STRING=${EVENTHUB_CONNECTION_STRING}
      - EVENTHUB_NAME=${EVENTHUB_NAME:-orders}
      - EVENTHUB_CONSUMER_GROUP=${EVENTHUB_CONSUMER_GROUP:-oms-consumer}

      # Blob checkpoint store (NOT the capture archive container)
      - AZURE_STORAGE_CONNECTION_STRING=${AZURE_STORAGE_CONNECTION_STRING}
      - CHECKPOINT_CONTAINER=${CHECKPOINT_CONTAINER:-eh-checkpoints}

      # MySQL
      - MYSQL_HOST=db
      - MYSQL_PORT=3306
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - internal
    restart: unless-stopped

  db:
    build:
      context: ./db
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - internal
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 3s
      retries: 30

networks:
  edge:
    name: oms_edge
  internal:
    name: oms_internal

volumes:
  dbdata:
    name: oms_dbdata
  
